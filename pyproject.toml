[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "agfi-docs"
authors = [{name = "Aggregated Finance", email = "team@aggregated.finance"}]
dynamic = ["version", "description"]
